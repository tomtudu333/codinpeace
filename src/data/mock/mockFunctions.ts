import type { FunctionRecord } from '../ModuleDataProvider';

export const mockFunctions: Record<string, FunctionRecord[]> = {
  'feat-user-register': [
    {
      id: 'fn-user-reg-create', featureId: 'feat-user-register',
      name: 'createUser', signature: 'createUser(input: CreateUserInput): Promise<User>',
      complexity: 12, lineStart: 45, lineEnd: 78,
      code: 'async function createUser(input: CreateUserInput): Promise<User> {\n  const existing = await findByEmail(input.email);\n  if (existing) throw new ConflictError(\'Email already in use\');\n  const hashed = await hashPassword(input.password);\n  const user = await User.create({ ...input, password: hashed });\n  await sendVerificationEmail(user);\n  return sanitizeUser(user);\n}',
      filePath: 'src/modules/user/registration.ts', documentation: 'complete',
      calls: ['fn-user-reg-findbyemail', 'fn-user-reg-hashpassword', 'fn-user-reg-sendverification'], calledBy: [],
      position: { x: 460, y: 40 }, width: 380, height: 320,
      accentColor: '#4f8cff',
      logicIcons: [
        { category: 'control-flow', name: 'If', count: 1, accentColor: '#fbbf24' },
        { category: 'error-handling', name: 'Try', count: 1, accentColor: '#f87171' },
        { category: 'database', name: 'Insert', count: 1, accentColor: '#34d399' },
        { category: 'utility', name: 'Async', count: 1, accentColor: '#a78bfa' },
      ],
      metadata: { paramCount: 1, returnCount: 1, dbCalls: 2, apiCalls: 1, estimatedCost: 'medium', testCoverage: 92, parameters: 'input: CreateUserInput', returnType: 'Promise<User>', description: 'Creates a new user account with hashed password and sends verification email.', tags: ['registration', 'user', 'email'], developerNotes: 'Uses bcrypt for password hashing. Email verification is sent asynchronously.' },
    },
    {
      id: 'fn-user-reg-findbyemail', featureId: 'feat-user-register',
      name: 'findByEmail', signature: 'findByEmail(email: string): Promise<User | null>',
      complexity: 5, lineStart: 80, lineEnd: 90,
      code: 'async function findByEmail(email: string): Promise<User | null> {\n  return User.findOne({ where: { email: email.toLowerCase() } });\n}',
      filePath: 'src/modules/user/registration.ts', documentation: 'complete',
      calls: [], calledBy: ['fn-user-reg-create', 'fn-user-login-auth'],
      position: { x: 40, y: 380 }, width: 340, height: 200,
      accentColor: '#60a5fa',
      logicIcons: [
        { category: 'database', name: 'Select', count: 1, accentColor: '#34d399' },
        { category: 'utility', name: 'String', count: 1, accentColor: '#a78bfa' },
      ],
      metadata: { paramCount: 1, returnCount: 1, dbCalls: 1, apiCalls: 0, estimatedCost: 'low', testCoverage: 95, parameters: 'email: string', returnType: 'Promise<User | null>', description: 'Looks up a user by their email address (case-insensitive).', tags: ['query', 'email'], developerNotes: 'Email is normalized to lowercase before query.' },
    },
    {
      id: 'fn-user-reg-validate', featureId: 'feat-user-register',
      name: 'validateRegistration', signature: 'validateRegistration(input: CreateUserInput): ValidationResult',
      complexity: 8, lineStart: 20, lineEnd: 42,
      code: 'function validateRegistration(input: CreateUserInput): ValidationResult {\n  const errors: string[] = [];\n  if (!input.email?.includes(\'@\')) errors.push(\'Invalid email\');\n  if (!input.password || input.password.length < 8) errors.push(\'Password too short\');\n  if (!input.name?.trim()) errors.push(\'Name is required\');\n  return { valid: errors.length === 0, errors };\n}',
      filePath: 'src/modules/user/registration.ts', documentation: 'complete',
      calls: [], calledBy: ['fn-user-reg-create'],
      position: { x: 40, y: 40 }, width: 380, height: 280,
      accentColor: '#fbbf24',
      logicIcons: [
        { category: 'control-flow', name: 'If', count: 3, accentColor: '#fbbf24' },
        { category: 'error-handling', name: 'Validation', count: 1, accentColor: '#34d399' },
        { category: 'utility', name: 'Array', count: 1, accentColor: '#a78bfa' },
      ],
      metadata: { paramCount: 1, returnCount: 1, dbCalls: 0, apiCalls: 0, estimatedCost: 'low', testCoverage: 98, parameters: 'input: CreateUserInput', returnType: 'ValidationResult', description: 'Validates registration input fields (email, password, name).', tags: ['validation', 'registration'], developerNotes: 'Pure validation function with no side effects.' },
    },
  ],
  'feat-user-login': [
    {
      id: 'fn-user-login-auth', featureId: 'feat-user-login',
      name: 'authenticate', signature: 'authenticate(email: string, password: string): Promise<AuthResult>',
      complexity: 15, lineStart: 10, lineEnd: 45,
      code: 'async function authenticate(email: string, password: string): Promise<AuthResult> {\n  const user = await findByEmail(email);\n  if (!user) throw new NotFoundError(\'User not found\');\n  const valid = await verifyPassword(password, user.password);\n  if (!valid) throw new AuthError(\'Invalid credentials\');\n  const token = generateToken(user);\n  return { user: sanitizeUser(user), token };\n}',
      filePath: 'src/modules/user/auth.ts', documentation: 'complete',
      calls: ['fn-user-reg-findbyemail', 'fn-user-login-verifypw', 'fn-user-login-gentoken'], calledBy: [],
      position: { x: 40, y: 40 }, width: 400, height: 340,
      accentColor: '#f472b6',
      logicIcons: [
        { category: 'control-flow', name: 'If', count: 2, accentColor: '#fbbf24' },
        { category: 'error-handling', name: 'Throw', count: 2, accentColor: '#f87171' },
        { category: 'auth', name: 'JWT', count: 1, accentColor: '#60a5fa' },
        { category: 'database', name: 'Select', count: 1, accentColor: '#34d399' },
      ],
      metadata: { paramCount: 2, returnCount: 1, dbCalls: 1, apiCalls: 0, estimatedCost: 'medium', testCoverage: 95, parameters: 'email: string, password: string', returnType: 'Promise<AuthResult>', description: 'Authenticates a user with email and password.', tags: ['auth', 'login'], developerNotes: 'Returns JWT token on success.' },
    },
    {
      id: 'fn-user-login-verifypw', featureId: 'feat-user-login',
      name: 'verifyPassword', signature: 'verifyPassword(plain: string, hashed: string): Promise<boolean>',
      complexity: 6, lineStart: 47, lineEnd: 55,
      code: 'async function verifyPassword(plain: string, hashed: string): Promise<boolean> {\n  return bcrypt.compare(plain, hashed);\n}',
      filePath: 'src/modules/user/auth.ts', documentation: 'complete',
      calls: [], calledBy: ['fn-user-login-auth'],
      position: { x: 40, y: 420 }, width: 360, height: 190,
      accentColor: '#fb923c',
      logicIcons: [
        { category: 'auth', name: 'Session', count: 1, accentColor: '#60a5fa' },
        { category: 'utility', name: 'Async', count: 1, accentColor: '#a78bfa' },
      ],
      metadata: { paramCount: 2, returnCount: 1, dbCalls: 0, apiCalls: 0, estimatedCost: 'low', testCoverage: 97, parameters: 'plain: string, hashed: string', returnType: 'Promise<boolean>', description: 'Compares a plain text password against a bcrypt hash.', tags: ['auth', 'password'], developerNotes: 'Wraps bcrypt.compare.' },
    },
  ],
  'feat-user-profile': [
    {
      id: 'fn-user-prof-update', featureId: 'feat-user-profile',
      name: 'updateProfile', signature: 'updateProfile(userId: string, data: ProfileUpdate): Promise<User>',
      complexity: 10, lineStart: 15, lineEnd: 40,
      code: 'async function updateProfile(userId: string, data: ProfileUpdate): Promise<User> {\n  const user = await User.findByPk(userId);\n  if (!user) throw new NotFoundError(\'User not found\');\n  if (data.name) user.name = data.name;\n  if (data.bio) user.bio = data.bio;\n  if (data.avatar) user.avatar = data.avatar;\n  await user.save();\n  return sanitizeUser(user);\n}',
      filePath: 'src/modules/user/profile.ts', documentation: 'complete',
      calls: ['fn-user-reg-findbyemail'], calledBy: [],
      position: { x: 40, y: 40 }, width: 380, height: 310,
      accentColor: '#34d399',
      logicIcons: [
        { category: 'control-flow', name: 'If', count: 3, accentColor: '#fbbf24' },
        { category: 'database', name: 'Update', count: 1, accentColor: '#34d399' },
        { category: 'error-handling', name: 'Throw', count: 1, accentColor: '#f87171' },
      ],
      metadata: { paramCount: 2, returnCount: 1, dbCalls: 2, apiCalls: 0, estimatedCost: 'low', testCoverage: 88, parameters: 'userId: string, data: ProfileUpdate', returnType: 'Promise<User>', description: 'Updates user profile fields (name, bio, avatar).', tags: ['profile', 'user'], developerNotes: 'Only updates provided fields.' },
    },
  ],
  'feat-cust-list': [
    {
      id: 'fn-cust-list-query', featureId: 'feat-cust-list',
      name: 'queryCustomers', signature: 'queryCustomers(filters: CustomerFilters): Promise<PaginatedResult<Customer>>',
      complexity: 14, lineStart: 30, lineEnd: 68,
      code: 'async function queryCustomers(filters: CustomerFilters): Promise<PaginatedResult<Customer>> {\n  const where = buildWhereClause(filters);\n  const { rows, count } = await Customer.findAndCountAll({\n    where, limit: filters.limit, offset: filters.offset,\n    order: [[filters.sortBy || \'createdAt\', filters.sortOrder || \'DESC\']],\n  });\n  return { data: rows, total: count, page: filters.page, pageSize: filters.limit };\n}',
      filePath: 'src/modules/customer/list.ts', documentation: 'complete',
      calls: ['fn-cust-list-buildwhere'], calledBy: [],
      position: { x: 40, y: 40 }, width: 420, height: 340,
      accentColor: '#f472b6',
      logicIcons: [
        { category: 'database', name: 'Select', count: 1, accentColor: '#34d399' },
        { category: 'control-flow', name: 'Return', count: 1, accentColor: '#fbbf24' },
        { category: 'utility', name: 'Async', count: 1, accentColor: '#a78bfa' },
        { category: 'performance', name: 'Cache', count: 1, accentColor: '#60a5fa' },
      ],
      metadata: { paramCount: 1, returnCount: 1, dbCalls: 1, apiCalls: 0, estimatedCost: 'medium', testCoverage: 90, parameters: 'filters: CustomerFilters', returnType: 'Promise<PaginatedResult<Customer>>', description: 'Queries customers with pagination, filtering, and sorting.', tags: ['customer', 'list', 'pagination'], developerNotes: 'Supports dynamic sorting and full-text search.' },
    },
    {
      id: 'fn-cust-list-buildwhere', featureId: 'feat-cust-list',
      name: 'buildWhereClause', signature: 'buildWhereClause(filters: CustomerFilters): WhereOptions',
      complexity: 7, lineStart: 15, lineEnd: 28,
      code: 'function buildWhereClause(filters: CustomerFilters): WhereOptions {\n  const where: WhereOptions = {};\n  if (filters.search) where.name = { [Op.iLike]: `%${filters.search}%` };\n  if (filters.status) where.status = filters.status;\n  if (filters.createdAfter) where.createdAt = { [Op.gte]: filters.createdAfter };\n  return where;\n}',
      filePath: 'src/modules/customer/list.ts', documentation: 'partial',
      calls: [], calledBy: ['fn-cust-list-query'],
      position: { x: 40, y: 420 }, width: 380, height: 240,
      accentColor: '#a78bfa',
      logicIcons: [
        { category: 'control-flow', name: 'If', count: 3, accentColor: '#fbbf24' },
        { category: 'database', name: 'Select', count: 1, accentColor: '#34d399' },
        { category: 'utility', name: 'Object', count: 1, accentColor: '#a78bfa' },
      ],
      metadata: { paramCount: 1, returnCount: 1, dbCalls: 0, apiCalls: 0, estimatedCost: 'low', testCoverage: 85, parameters: 'filters: CustomerFilters', returnType: 'WhereOptions', description: 'Builds a Sequelize WHERE clause from filter parameters.', tags: ['query', 'filter'], developerNotes: 'Uses Op.iLike for case-insensitive search.' },
    },
  ],
  'feat-order-create': [
    {
      id: 'fn-order-create-place', featureId: 'feat-order-create',
      name: 'placeOrder', signature: 'placeOrder(input: CreateOrderInput): Promise<Order>',
      complexity: 18, lineStart: 50, lineEnd: 95,
      code: 'async function placeOrder(input: CreateOrderInput): Promise<Order> {\n  const cart = await Cart.findByPk(input.cartId, { include: [CartItem] });\n  if (!cart) throw new NotFoundError(\'Cart not found\');\n  const total = calculateTotal(cart.items);\n  const order = await Order.create({\n    userId: input.userId, status: \'pending\', total,\n    shippingAddress: input.shippingAddress,\n  });\n  for (const item of cart.items) {\n    await OrderItem.create({ orderId: order.id, ...item });\n    await decrementStock(item.productId, item.quantity);\n  }\n  await cart.destroy();\n  return order;\n}',
      filePath: 'src/modules/order/order.ts', documentation: 'complete',
      calls: ['fn-order-create-total', 'fn-order-create-decrstock'], calledBy: [],
      position: { x: 40, y: 40 }, width: 440, height: 380,
      accentColor: '#34d399',
      logicIcons: [
        { category: 'control-flow', name: 'Loop', count: 1, accentColor: '#fbbf24' },
        { category: 'control-flow', name: 'If', count: 1, accentColor: '#fbbf24' },
        { category: 'database', name: 'Insert', count: 3, accentColor: '#34d399' },
        { category: 'database', name: 'Update', count: 1, accentColor: '#34d399' },
        { category: 'error-handling', name: 'Throw', count: 1, accentColor: '#f87171' },
      ],
      metadata: { paramCount: 1, returnCount: 1, dbCalls: 5, apiCalls: 0, estimatedCost: 'high', testCoverage: 90, parameters: 'input: CreateOrderInput', returnType: 'Promise<Order>', description: 'Creates an order from a cart, decrements stock, and clears the cart.', tags: ['order', 'cart', 'stock'], developerNotes: 'Runs within a database transaction in production.' },
    },
    {
      id: 'fn-order-create-total', featureId: 'feat-order-create',
      name: 'calculateTotal', signature: 'calculateTotal(items: CartItem[]): number',
      complexity: 6, lineStart: 97, lineEnd: 108,
      code: 'function calculateTotal(items: CartItem[]): number {\n  return items.reduce((sum, item) => sum + item.price * item.quantity, 0);\n}',
      filePath: 'src/modules/order/order.ts', documentation: 'complete',
      calls: [], calledBy: ['fn-order-create-place'],
      position: { x: 520, y: 40 }, width: 360, height: 200,
      accentColor: '#60a5fa',
      logicIcons: [
        { category: 'utility', name: 'Math', count: 1, accentColor: '#a78bfa' },
        { category: 'utility', name: 'Array', count: 1, accentColor: '#a78bfa' },
      ],
      metadata: { paramCount: 1, returnCount: 1, dbCalls: 0, apiCalls: 0, estimatedCost: 'low', testCoverage: 100, parameters: 'items: CartItem[]', returnType: 'number', description: 'Calculates the total price of all cart items.', tags: ['order', 'math'], developerNotes: 'Pure reduce function.' },
    },
    {
      id: 'fn-order-create-decrstock', featureId: 'feat-order-create',
      name: 'decrementStock', signature: 'decrementStock(productId: string, quantity: number): Promise<void>',
      complexity: 8, lineStart: 110, lineEnd: 122,
      code: 'async function decrementStock(productId: string, quantity: number): Promise<void> {\n  const product = await Product.findByPk(productId);\n  if (!product) throw new NotFoundError(\'Product not found\');\n  if (product.stock < quantity) throw new ConflictError(\'Insufficient stock\');\n  await product.update({ stock: product.stock - quantity });\n}',
      filePath: 'src/modules/order/order.ts', documentation: 'partial',
      calls: [], calledBy: ['fn-order-create-place'],
      position: { x: 520, y: 280 }, width: 360, height: 250,
      accentColor: '#fb923c',
      logicIcons: [
        { category: 'database', name: 'Update', count: 1, accentColor: '#34d399' },
        { category: 'control-flow', name: 'If', count: 2, accentColor: '#fbbf24' },
        { category: 'error-handling', name: 'Throw', count: 2, accentColor: '#f87171' },
      ],
      metadata: { paramCount: 2, returnCount: 0, dbCalls: 2, apiCalls: 0, estimatedCost: 'medium', testCoverage: 82, parameters: 'productId: string, quantity: number', returnType: 'Promise<void>', description: 'Decrements product stock, throws on insufficient stock.', tags: ['stock', 'inventory', 'order'], developerNotes: 'Should be called within a transaction.' },
    },
  ],
  'feat-pay-process': [
    {
      id: 'fn-pay-process-charge', featureId: 'feat-pay-process',
      name: 'processPayment', signature: 'processPayment(orderId: string, payment: PaymentInput): Promise<PaymentResult>',
      complexity: 20, lineStart: 20, lineEnd: 65,
      code: 'async function processPayment(orderId: string, payment: PaymentInput): Promise<PaymentResult> {\n  const order = await Order.findByPk(orderId);\n  if (!order) throw new NotFoundError(\'Order not found\');\n  const gateway = resolveGateway(payment.method);\n  const charge = await gateway.charge({ amount: order.total, currency: \'USD\', source: payment.token });\n  await Payment.create({ orderId, amount: order.total, method: payment.method, status: \'completed\', transactionId: charge.id });\n  await order.update({ status: \'confirmed\' });\n  return { success: true, transactionId: charge.id };\n}',
      filePath: 'src/modules/payment/processor.ts', documentation: 'complete',
      calls: ['fn-pay-process-resolvegw'], calledBy: [],
      position: { x: 40, y: 40 }, width: 420, height: 360,
      accentColor: '#a78bfa',
      logicIcons: [
        { category: 'network', name: 'REST', count: 1, accentColor: '#60a5fa' },
        { category: 'database', name: 'Insert', count: 1, accentColor: '#34d399' },
        { category: 'database', name: 'Update', count: 1, accentColor: '#34d399' },
        { category: 'error-handling', name: 'Throw', count: 1, accentColor: '#f87171' },
        { category: 'control-flow', name: 'If', count: 1, accentColor: '#fbbf24' },
      ],
      metadata: { paramCount: 2, returnCount: 1, dbCalls: 2, apiCalls: 1, estimatedCost: 'high', testCoverage: 88, parameters: 'orderId: string, payment: PaymentInput', returnType: 'Promise<PaymentResult>', description: 'Processes a payment through the appropriate gateway.', tags: ['payment', 'gateway'], developerNotes: 'Supports Stripe and PayPal.' },
    },
    {
      id: 'fn-pay-process-resolvegw', featureId: 'feat-pay-process',
      name: 'resolveGateway', signature: 'resolveGateway(method: string): PaymentGateway',
      complexity: 4, lineStart: 67, lineEnd: 78,
      code: 'function resolveGateway(method: string): PaymentGateway {\n  const gateways: Record<string, PaymentGateway> = {\n    stripe: new StripeGateway(), paypal: new PayPalGateway(),\n  };\n  const gw = gateways[method];\n  if (!gw) throw new Error(`Unsupported payment method: ${method}`);\n  return gw;\n}',
      filePath: 'src/modules/payment/processor.ts', documentation: 'complete',
      calls: [], calledBy: ['fn-pay-process-charge'],
      position: { x: 40, y: 440 }, width: 360, height: 220,
      accentColor: '#34d399',
      logicIcons: [
        { category: 'control-flow', name: 'If', count: 1, accentColor: '#fbbf24' },
        { category: 'error-handling', name: 'Throw', count: 1, accentColor: '#f87171' },
        { category: 'utility', name: 'Object', count: 1, accentColor: '#a78bfa' },
      ],
      metadata: { paramCount: 1, returnCount: 1, dbCalls: 0, apiCalls: 0, estimatedCost: 'low', testCoverage: 94, parameters: 'method: string', returnType: 'PaymentGateway', description: 'Resolves a payment method string to a gateway instance.', tags: ['payment', 'gateway'], developerNotes: 'Registry pattern for gateway resolution.' },
    },
  ],
  'feat-auth-sso': [
    {
      id: 'fn-auth-sso-handle', featureId: 'feat-auth-sso',
      name: 'handleSSOCallback', signature: 'handleSSOCallback(provider: string, code: string): Promise<AuthResult>',
      complexity: 16, lineStart: 12, lineEnd: 52,
      code: 'async function handleSSOCallback(provider: string, code: string): Promise<AuthResult> {\n  const config = getProviderConfig(provider);\n  const tokens = await exchangeCode(code, config);\n  const profile = await fetchProfile(provider, tokens.accessToken);\n  let user = await User.findOne({ where: { ssoId: profile.id, ssoProvider: provider } });\n  if (!user) user = await User.create({ ssoId: profile.id, ssoProvider: provider, name: profile.name, email: profile.email });\n  const token = generateToken(user);\n  return { user: sanitizeUser(user), token };\n}',
      filePath: 'src/modules/auth/sso.ts', documentation: 'complete',
      calls: ['fn-user-login-gentoken'], calledBy: [],
      position: { x: 40, y: 40 }, width: 440, height: 360,
      accentColor: '#f87171',
      logicIcons: [
        { category: 'network', name: 'REST', count: 2, accentColor: '#60a5fa' },
        { category: 'auth', name: 'JWT', count: 1, accentColor: '#60a5fa' },
        { category: 'control-flow', name: 'If', count: 1, accentColor: '#fbbf24' },
        { category: 'database', name: 'Select', count: 1, accentColor: '#34d399' },
        { category: 'database', name: 'Insert', count: 1, accentColor: '#34d399' },
      ],
      metadata: { paramCount: 2, returnCount: 1, dbCalls: 2, apiCalls: 2, estimatedCost: 'high', testCoverage: 85, parameters: 'provider: string, code: string', returnType: 'Promise<AuthResult>', description: 'Handles the SSO callback from an external identity provider.', tags: ['auth', 'sso', 'oidc'], developerNotes: 'Supports SAML and OIDC providers.' },
    },
  ],
  'feat-auth-oauth': [
    {
      id: 'fn-auth-oauth-authorize', featureId: 'feat-auth-oauth',
      name: 'authorizeClient', signature: 'authorizeClient(clientId: string, scopes: string[], redirectUri: string): Promise<AuthCode>',
      complexity: 14, lineStart: 30, lineEnd: 62,
      code: 'async function authorizeClient(clientId: string, scopes: string[], redirectUri: string): Promise<AuthCode> {\n  const client = await OAuthClient.findByPk(clientId);\n  if (!client) throw new NotFoundError(\'Client not found\');\n  if (client.redirectUri !== redirectUri) throw new AuthError(\'Redirect URI mismatch\');\n  const validScopes = scopes.filter(s => client.allowedScopes.includes(s));\n  const code = crypto.randomBytes(32).toString(\'hex\');\n  await AuthCode.create({ code, clientId, scopes: validScopes, expiresAt: new Date(Date.now() + 600000) });\n  return { code, redirectUri };\n}',
      filePath: 'src/modules/auth/oauth.ts', documentation: 'complete',
      calls: [], calledBy: [],
      position: { x: 40, y: 40 }, width: 440, height: 350,
      accentColor: '#f87171',
      logicIcons: [
        { category: 'auth', name: 'Permission', count: 1, accentColor: '#60a5fa' },
        { category: 'control-flow', name: 'If', count: 2, accentColor: '#fbbf24' },
        { category: 'error-handling', name: 'Throw', count: 2, accentColor: '#f87171' },
        { category: 'database', name: 'Insert', count: 1, accentColor: '#34d399' },
        { category: 'utility', name: 'String', count: 1, accentColor: '#a78bfa' },
      ],
      metadata: { paramCount: 3, returnCount: 1, dbCalls: 2, apiCalls: 0, estimatedCost: 'medium', testCoverage: 80, parameters: 'clientId: string, scopes: string[], redirectUri: string', returnType: 'Promise<AuthCode>', description: 'Authorizes an OAuth2 client with requested scopes.', tags: ['auth', 'oauth2', 'authorization'], developerNotes: 'Authorization codes expire after 10 minutes.' },
    },
  ],
};
